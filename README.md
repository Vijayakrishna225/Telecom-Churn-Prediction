# Telecom-Churn-Prediction
Executed an end-to-end project to predict customer churn, processing the telecom dataset and applying StandardScaler. Trained and compared seven classification models (including XGBoost) to isolate key churn factors. The final model was persisted, providing a deployable solution for proactive customer retention strategies.
